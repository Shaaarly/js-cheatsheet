// 07-functions — Ejercicio 2
// Sin datos: multiplicador(factor) devuelve función (n) => factor * n

function multiplicador(factor) {
    return (n) => valor = n * factor;
}

const doble = multiplicador(3)
console.log(doble(5))