// 19-react-desde-cero — Ejercicio 5
// Formulario controlado: dos inputs (nombre, email) en estado; onSubmit que muestre por consola o en un <p> los valores.
// Usa value={estado} y onChange para cada input; onSubmit en el <form> con e.preventDefault().
